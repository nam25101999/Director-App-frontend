<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Báo cáo tổng hợp</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%- include('partials/header') %>

  <div class="container py-4">
    <h1 class="mb-4 text-center text-primary">📊 Báo cáo tổng hợp</h1>

    <!-- Học tập -->
    <div class="card mb-4">
      <div class="card-header bg-info text-white">1. Học tập</div>
      <div class="card-body">
        <h5 class="text-dark">🎓 Điểm học tập</h5>
        <ul class="list-group mb-3">
          <% grades.forEach(item => { %>
            <li class="list-group-item">
              <strong><%= item.studentName %></strong> - <%= item.course %>: <%= item.score %>
            </li>
          <% }) %>
        </ul>

        <h5 class="text-dark">📝 Kết quả kiểm tra</h5>
        <ul class="list-group">
          <% testScores.forEach(score => { %>
            <li class="list-group-item">
              <strong><%= score.studentName %></strong> - <%= score.subject %>: <%= score.score %>
            </li>
          <% }) %>
        </ul>
      </div>
    </div>

    <!-- Thực tập -->
    <div class="card mb-4">
      <div class="card-header bg-success text-white">2. Thực tập</div>
      <div class="card-body">
        <h5 class="text-dark">📋 Đăng ký thực tập</h5>
        <ul class="list-group mb-3">
          <% internships.forEach(intern => { %>
            <li class="list-group-item">
              <strong><%= intern.studentName %></strong> đăng ký tại <%= intern.companyName %>
            </li>
          <% }) %>
        </ul>

        <h5 class="text-dark">🗂️ Báo cáo thực tập</h5>
        <ul class="list-group">
          <% internshipReports.forEach(report => { %>
            <li class="list-group-item">
              <strong><%= report.studentName %></strong>: <%= report.summary %>
            </li>
          <% }) %>
        </ul>
      </div>
    </div>

    <!-- Giảng dạy -->
    <div class="card mb-4">
      <div class="card-header bg-warning text-dark">3. Giảng dạy</div>
      <div class="card-body">
        <h5 class="text-dark">👩‍🏫 Phân công giảng dạy</h5>
        <ul class="list-group mb-3">
          <% teacherAssignments.forEach(assign => { %>
            <li class="list-group-item">
              <strong><%= assign.teacherName %></strong> dạy <%= assign.subject %> lớp <%= assign.className %>
            </li>
          <% }) %>
        </ul>

        <h5 class="text-dark">📆 Lịch giảng dạy</h5>
        <ul class="list-group">
          <% teachingSchedule.forEach(schedule => { %>
            <li class="list-group-item">
              <strong><%= schedule.teacherName %></strong>: <%= schedule.date %> - <%= schedule.className %> - <%= schedule.subject %>
            </li>
          <% }) %>
        </ul>
      </div>
    </div>
  </div>
  <%- include('partials/footer') %>

</body>
</html>
