<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Kết Quả Đánh Giá Chất Lượng</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%- include('partials/header') %>
  <div class="container mt-5">
    <h2 class="text-center mb-4">Bảng Kết Quả Đánh Giá Chất Lượng</h2>

    <div class="table-responsive">
      <table class="table table-bordered table-striped table-hover align-middle">
        <thead class="table-dark text-center">
          <tr>
            <th>ID</th>
            <th>Assessment ID</th>
            <th>Mã đối tượng</th>
            <th>Loại đối tượng</th>
            <th>Điểm</th>
            <th>Nhận xét</th>
            <th>Ngày nộp</th>
          </tr>
        </thead>
        <tbody>
          <% if (results.length === 0) { %>
            <tr>
              <td colspan="7" class="text-center">Không có dữ liệu đánh giá.</td>
            </tr>
          <% } else { %>
            <% results.forEach(r => { %>
              <tr>
                <td class="text-center"><%= r.ResultID %></td>
                <td class="text-center"><%= r.AssessmentID %></td>
                <td class="text-center"><%= r.EntityID %></td>
                <td class="text-center"><%= r.EntityType %></td>
                <td class="text-end"><%= r.Score %></td>
                <td><%= r.Comments || '' %></td>
                <td class="text-center"><%= r.SubmissionDate %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
  <%- include('partials/footer') %>

</body>
</html>
