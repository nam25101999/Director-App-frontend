<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Đăng ký thực tập</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <%- include('partials/header') %>
  <div class="container my-5">
    <h1 class="mb-4 text-center">📝 Danh sách đăng ký thực tập</h1>

    <div class="table-responsive">
      <table class="table table-striped table-bordered align-middle">
        <thead class="table-dark">
          <tr>
            <th scope="col">InternshipID</th>
            <th scope="col">StudentID</th>
            <th scope="col">CompanyID</th>
            <th scope="col">Position</th>
            <th scope="col">StartDate</th>
            <th scope="col">EndDate</th>
            <th scope="col">Status</th>
            <th scope="col">ExpectedSalary</th>
            <th scope="col">ApprovedBy</th>
            <th scope="col">RegulationID</th>
            <th scope="col">RegistrationTime</th>
            <th scope="col">RequiredDocuments</th>
            <th scope="col">Notes</th>
          </tr>
        </thead>
        <tbody>
          <% if (internships.length === 0) { %>
            <tr>
              <td colspan="13" class="text-center">Không có dữ liệu đăng ký thực tập.</td>
            </tr>
          <% } else { %>
            <% internships.forEach(item => { %>
              <tr>
                <td><%= item.InternshipID %></td>
                <td><%= item.StudentID %></td>
                <td><%= item.CompanyID %></td>
                <td><%= item.Position %></td>
                <td><%= new Date(item.StartDate).toLocaleDateString('vi-VN') %></td>
                <td><%= new Date(item.EndDate).toLocaleDateString('vi-VN') %></td>
                <td><%= item.Status %></td>
                <td><%= item.ExpectedSalary ? item.ExpectedSalary.toLocaleString() : '' %></td>
                <td><%= item.ApprovedBy %></td>
                <td><%= item.RegulationID %></td>
                <td><%= new Date(item.RegistrationTime).toLocaleString('vi-VN') %></td>
                <td><%= item.RequiredDocuments %></td>
                <td><%= item.Notes %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
  <%- include('partials/footer') %>
</body>
</html>
