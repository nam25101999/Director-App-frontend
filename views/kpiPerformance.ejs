<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Hiệu Suất KPI</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap 5 CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <%- include('partials/header') %>
  <div class="container mt-5">
    <h2 class="text-center mb-4">Bảng Hiệu Suất KPI</h2>

    <div class="table-responsive">
      <table class="table table-bordered table-striped table-hover align-middle">
        <thead class="table-dark text-center">
          <tr>
            <th>ID</th>
            <th>KPI ID</th>
            <th>Mã đối tượng</th>
            <th>Loại đối tượng</th>
            <th>Giai đoạn</th>
            <th>Điểm</th>
            <th>Nhận xét</th>
            <th>Ngày đánh giá</th>
          </tr>
        </thead>
        <tbody>
          <% if (!performances || performances.length === 0) { %>
            <tr>
              <td colspan="8" class="text-center">Không có dữ liệu KPI.</td>
            </tr>
          <% } else { %>
            <% performances.forEach(p => { %>
              <tr>
                <td class="text-center"><%= p.PerformanceID %></td>
                <td class="text-center"><%= p.KPIID %></td>
                <td class="text-center"><%= p.EntityID %></td>
                <td class="text-center"><%= p.EntityType %></td>
                <td class="text-center"><%= p.EvaluationPeriod %></td>
                <td class="text-end"><%= p.Score %></td>
                <td><%= p.Comments || '' %></td>
                <td class="text-center"><%= p.EvaluationDate %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
  <%- include('partials/footer') %>

</body>
</html>
